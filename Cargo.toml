[workspace]
resolver = "2"
members = [
    "packages/ui",
    "packages/web",
    "packages/desktop",
    "packages/mobile",
    "packages/api", 
    "packages/calculator", 
    "packages/db",
    "packages/migration"
]

[workspace.dependencies]
dioxus = { version = "0.7.1" }

# workspace
ui = { path = "packages/ui" }
api = { path = "packages/api" }
calculator = { path = "packages/calculator"}
db = { path="packages/db"}
migration = { path="packages/migration"}
