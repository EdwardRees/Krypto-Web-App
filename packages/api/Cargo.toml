[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4.42"
dioxus = { workspace = true, features = ["fullstack"] }
rand = "0.9.2"
calculator = "calculator"

[features]
server = ["dioxus/server"]
